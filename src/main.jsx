import React from "react";
import { createRoot } from "react-dom/client";
import "./style.css";
import CelpeBras from "./CelpeBras";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CelpeBras />
  </React.StrictMode>
);
